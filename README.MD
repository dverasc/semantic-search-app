# source code for semantic search application case study

# this entire repo assumes you've installed docker and have llama 2 (or equivalent model) running locally using ollama as well as set up streamlit 